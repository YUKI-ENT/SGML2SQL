# 相互作用相手のコード化と未解決管理の提案

作成日: 2026-09-23。設計案であり、以下の状態・テーブル・処理は未実装。
添付の `sgml2sql-interaction-requirements.md` を参考資料として検討した。

追記: この設計の後、読み取り専用の評価実装を追加した。実装済み範囲・実DBでの検証結果・実行手順は
[相互作用コード化ガイド](SGML_INTERACTION_APPLICATION_GUIDE.md) を参照。以下の調査時点の記録は履歴として残す。

## 目的

当日処方Aと他院薬Bを双方向に照合するため、添付文書に書かれた相手薬をコードに対応付ける。
最優先は、対応できた範囲と、まだ判断できない範囲を原文に戻って確認できること。
コードが見つからないことを「相互作用なし」に変換しない。

## 現行コードで確認できたこと

- 調査時のHEADは `696b5d0`。資料に記載されたリビジョンと一致している。
- `21_sgml2rawdata.py` の `lang_text_all` は `itertext()` を区切りなしで結合している。
- `extract_partner_group_and_items` は最初の直下 `Detail` を代表見出しとして採用し、直下のリスト項目に共通で付けている。
- `normalize_partner_label` は「等」「など」を除去するため、例示・非網羅性の情報が平坦化結果に残らない。
- `22_build_sgml_interaction.py` は既存テーブルを `DROP ... CASCADE` し、そのトランザクションを終了してから投入している。新規投入に失敗した際に旧データへ戻せる構成ではない。
- 原文XMLは `sgml_rawdata.doc_xml` に保存する実装がある。再抽出はこの原文を入口にできる。

これはコード上の確認であり、提示されたマオウ含有製剤の原文構造・誤抽出原因の実証ではない。
作業フォルダーの `SGML` には元XMLがなく、参考CSVも未提供。実データでの成功件数・失敗件数は未測定。
既存 `.venv` は参照先Pythonが存在せず起動できなかった。DB照会は実行に到達していない。

## 状態は三つの軸に分ける

| 軸 | 値 | 意味 |
| --- | --- | --- |
| 対象範囲の解決 | resolved / partial / unresolved | 相手記載が指す範囲の全部・一部・未確定 |
| コード対応の採用 | accepted / needs_review / rejected | 個々の対応根拠を採用できるか |
| 適用条件 | none / supported / unresolved | 条件なし・機械評価可能・評価不能 |

`supported` は「今回の患者・薬剤に条件が成立した」という意味ではない。
照合時には別途、条件成立・不成立・情報不足を評価する。
例えば「名称のコード対応は確定、投与経路は不明」を表現する。

対象範囲は相手記載ごとに管理する。同じ行の具体名だけ解決できても、併記された群まで解決済みにしない。
行単位の集計は、全対象がresolvedならresolved、一部のみ確定ならpartial、確定部分がなければunresolvedとする。
群の例示薬が解決しても、群全体はresolvedにならない。
候補があるだけの場合はunresolvedのままにする。抽出欠落・解析失敗は別途不明記録として必ず残す。

## 最初にコード化する範囲

1. 元XMLの境界から切り出せた具体名を、一般名・標準名・販売名の正規化一致で照合する。
2. 表記ゆれは確認済み別名辞書で補う。名称の中黒・ハイフンを一律に分割しない。
3. 部分一致・分類一致は確認候補にとどめる。曖昧な一致を確定対応へ昇格させない。
4. 群、例示、配合剤、除外、経路・剤形限定、構造不明は原文とともに保持する。

自動採用には、抽出境界・対象の意味・辞書の対応範囲が検証済みであることが必要。
名称一致だけを理由に、販売名限定から同じYJ先頭7桁の全製品へ広げない。
検索キーは相手YJ先頭7桁を使っても、許可する全桁コードを残す。
自薬側はまず添付文書に紐づく全桁コードを適用単位とし、7桁集約の可否は別に検証する。

## 何が成功・失敗したかを見る成果物

相互作用原文、抽出した相手記載、コード対応を別のデータとして保持する。
不明記録はコードが0件でも存在し、一覧・集計から消えない。

確認用一覧に最低限必要な列:

- 元文書・版・自薬全桁YJ・併用禁忌/注意・原文・XML位置
- 相手記載・群/具体名/不明・例示/除外の役割・親対象
- 解決状態・未解決理由・候補コード・採用コード
- 照合方法・辞書の版と出典・条件原文・条件の評価状態
- レビュー担当・日時・判断根拠・次の対応

原文と位置に基づく追跡キーと内容ハッシュを持たせる。
再採番IDで手動判断を紐付けず、原文・条件・辞書の変更時は影響する採用判断を再確認する。

## 不明を減らす運用

| 未解決理由 | 次の対応 | 解消とみなす条件 |
| --- | --- | --- |
| XML境界・見出しの解釈不能 | 元XMLを確認し、抽出器と回帰用フィクスチャを修正 | 列挙・配合剤・見出し範囲を保持できる |
| 名称辞書に未登録 | 原文と薬剤マスターを確認し、出典付き別名を追加 | 対象範囲を説明できる対応が得られる |
| 名称が曖昧・複数候補 | 規格、成分、製品、経路を確認 | 正しい複数対象と曖昧な複数候補を区別できる |
| 群の定義・網羅性が不明 | 出典付き群辞書を作り、適用範囲をレビュー | 例示だけでなく群全体の範囲を検証できる |
| 除外・条件を評価できない | 条件の構造化と必要な入力情報を定義 | 除外・条件を機械評価できる |
| 入力不足・解析エラー | 入力を補い、エラー原因を修正して再実行 | 当該文書の処理完了を確認できる |

優先度は、併用禁忌の未解決、よく照合される薬剤、同じ修正で多くの記載を解消できるものを中心に付ける。
一件ごとのコード手入力より、再利用可能な抽出規則・別名辞書・群辞書を修正する。
辞書更新後は保存済み抽出結果から再解決する。ただし抽出規則の変更時は原文から再抽出する。
LLMを使う場合も原文付き候補作成の補助とし、推測だけで採用済みにしない。

## OQSDrugでの扱い

Aの記載からB、Bの記載からAを照合し、原文キーで重複をまとめる。
照合結果と未評価範囲は同時に表示できるようにする。

| 状況 | 表示例 |
| --- | --- |
| 採用済み対応に一致し、必要条件も成立 | 併用禁忌/併用注意：原文と根拠を表示 |
| 候補のみ一致、または条件情報が不足 | 要確認：候補根拠・不足情報を表示 |
| 一致はないが未解決記載が残る | コード一致なし／未評価の記載あり |
| 評価できた範囲で一致なし | 評価した範囲ではコード一致なし |
| 原文未取得・薬剤コード不明・処理失敗 | チェック未完了：理由を表示 |

未解決の群があるだけで全薬剤ペアに警告を複製せず、当該薬剤の未評価項目としてまとめる。
明示的除外や却下済み対応を、名称の部分一致によって再び陽性にしない。
相互作用記載が0件の場合も、文書未取得・処理失敗と、原文を確認して記載がなかった場合を分ける。

## 試作から連携まで

1. 元XMLと全桁YJ付き名称マスターで小規模な評価セットを作る。資料の問題例を含める。
2. 公開DBを変更しない抽出・解決試作で、全相手記載と未解決理由を出力する。
3. 成功・部分成功・不明をレビューし、誤対応と見落としを測定する。
4. 採用規則と状態遷移を固定した後、世代付き関連テーブルとOQSDrug向け取得SQLを実装する。
5. 原文・対象・対応を同一世代で検証し、一括切替する。読取側は世代を固定する。
6. 群辞書と条件評価を段階的に拡張し、変更前後の差分と再レビュー対象を出力する。

集計は処理対象文書数、正常/失敗/対象章なし、相互作用行数、相手記載数を分ける。
解決率の分母は抽出された相手記載数と明示し、文書の抽出失敗数を併記する。
コード対応件数や例示薬件数の増加を、群の解決率向上と混同しない。
サンプルを人手で確認した誤対応・見落としと、サンプルの選び方も報告する。

この提案では既存の21・22処理、DB、OQSDrugの実装は変更していない。
